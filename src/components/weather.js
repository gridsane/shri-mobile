var React = require('react');

var Weather = React.createClass({
  render: function () {
    return (
      <div className="weather">
        Weather Component
      </div>
    );
  }
});

module.exports = Weather;
