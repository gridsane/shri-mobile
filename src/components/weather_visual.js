var React = require('react');

var WeatherVisual = React.createClass({
    render: function () {
        return (
            <div className="weather-visual">Visual</div>
        );
    }
});

module.exports = WeatherVisual;
